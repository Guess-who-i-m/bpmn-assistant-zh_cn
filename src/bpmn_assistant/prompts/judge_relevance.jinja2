用户提出的流程构建需求：

{{ message_history }}

---

从本地知识库中检索到的知识：

{{ knowledge }}

---

请根据以下步骤来判断用户的流程构建需求和本地知识库检索内容是否相关：
请逐个评估以下检索内容与问题的关联性，判断标准：
- 是否包含问题关键词（权重30%）
- 是否直接解答问题核心（权重50%）
- 是否提供有效背景信息（权重20%）
为每个段落打分（0-100分），低于80分视为不相关


你的响应必须使用JSON格式，并且JSON中只包含一个键，这个键是"relevant"
- 用户的流程构建需求和本地知识库检索内容相关，值为True
- 用户的流程构建需求和本地知识库检索内容不相关，值为False

用户的流程构建需求和本地知识库检索内容相关：
```json
{
    "relevant": "True",
}
```

用户的流程构建需求和本地知识库检索内容不相关：
```json
{
    "relevant": "False",
}
```